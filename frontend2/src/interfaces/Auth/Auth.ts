export type LoginInterface = {
  email: string;
  password: string;
};

export type RegisterInterface = {
  email: string;
  password: string;
  name: string;
  surname: string;
};

export type ReqRegisterInterface = {
  registerUser: string;
};
